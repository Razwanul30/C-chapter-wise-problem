#include <stdio.h>

#define PI 3.1416  // Defining PI as a symbolic constant

int main() {
    double radius = 5.0;  // Assumed radius
    double area = PI * radius * radius;
    
    printf("Radius: %.2f\n", radius);
    printf("Area of the Circle: %.2f\n", area);
    
return 0;
}